export const GLOBE_SCALE = 0.9;
export const CAMERA_DISTANCE = 365;
/** Fit radius for default view — earth + 3D cloud shell so clouds stay in frame. */
export const DEFAULT_VIEW_RADIUS_SCALE = 1.2;
export const DEFAULT_FOV_MARGIN = 0.78;
export const FOCUSED_FOV_MARGIN = 0.72;
/** Camera distance from globe center when a marker is clicked. */
export const FOCUS_CAMERA_DISTANCE = 198;
export const FOCUS_ANIMATION_SECONDS = 1.25;
/** Closest / farthest scroll zoom (distance from globe center). */
export const ZOOM_MIN_DISTANCE = 175;
export const ZOOM_MAX_DISTANCE = 395;
/** Zooming out past this clears the focused marker label. */
export const ZOOM_OUT_RESET_THRESHOLD = 335;
export const POINT_ALTITUDE = 0.008;
export const POINT_RADIUS = 0.24;
/** Screen-space radius for clicking / hovering markers (px). */
export const POINT_PICK_RADIUS_PX = 34;
export const POINT_HOVER_RADIUS_PX = 42;
